sleep 100 &: 백그라운드에서 100초 동안 실행되는 두 개의 sleep 명령어를 실행한다. 각각은 백그라운드에서 실행되기 때문에 프롬프트는 즉시 반환된다.

jobs: 현재 쉘에서 실행 중인 작업의 목록을 표시한다. 두 개의 백그라운드 sleep 작업이 출력된다.

ps -f: 현재 실행 중인 모든 프로세스의 상세 정보를 표시한다. 각 프로세스에는 PID (프로세스 ID), PPID (부모 프로세스 ID), CMD (명령어) 등이 출력된다.

kill %1: 현재 쉘에서 백그라운드 작업 중 첫 번째 작업에 대해 종료 신호를 보낸다. 첫 번째 작업은 sleep 100이므로 해당 프로세스가 종료된다.
